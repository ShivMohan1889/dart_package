import 'package:pdf/widgets.dart';

class AuditSectionRow extends StatelessWidget {
  final String? sectionName;
  final String? sectionDescription;

  AuditSectionRow({
    this.sectionName,
    this.sectionDescription,
  });
  @override
  Widget build(Context context) {
    return Column(
      children: [
        Container(),
      ],
    );
  }
}
